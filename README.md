The program.txt file is the input file that the scanner reads. If you want to test out the scanner, just change the contents of the .txt file.

Reminder to push new changes to a separate branch (not main).
